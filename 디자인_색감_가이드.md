# 디자인·색감 가이드 (라이트 리프레시)

> 보고서 HTML의 색·헤더·강조 규율. 표준기준서 §9(디자인·양식 표준)를 실제 토큰으로 구체화한다.
> 계기: 골드+다크네이비 헤더가 전 슬라이드에서 **칙칙·과강조 → 전달력 저하**. 밝게 리프레시.
> 기준일 2026-07-12 · 최초 적용: `cashflow-report/양산_사업수지_보고서.html`

---

## 1. 그동안 섞였던 색 체계 (정리)

| 출처 | 색 | 문제 |
|---|---|---|
| 표준기준서 §9 | 네이비 `#13243f` + **스카이 `#3b86d1`** | 원래 추천은 밝은 스카이 액센트 |
| CLAUDE.md "다방" | 골드 `#b3873a` / 그래파이트네이비 / 그레이 | 브랜드 골드 |
| 양산 report(구) | 골드 + **다크네이비 헤더 띠(전 슬라이드)** | **칙칙의 주범** |

→ **해소안**: 표준기준서 §9의 네이비+스카이를 기본으로, 골드는 **얇은 브랜드 액센트로 절제**. 다크는 **표지·마무리(북엔드)에만**.

---

## 2. 확정 토큰 (`:root`)

```css
--ink:#233043;                      /* 본문 잉크(약네이비) */
--navy:#13243f; --navy2:#2a3f63;    /* 구조·타이틀·표 머리행 */
--gold:#b3873a; --gold-d:#96702a; --gold-l:#f6efe0; --gold-b:#d8c191;  /* 브랜드 액센트(절제) */
--sky:#3b86d1; --sky-d:#2f6fb0; --sky-l:#eaf3fb;   /* 데이터·아이브로우 액센트 */
--paper:#ffffff; --mut:#6b7684; --line:#e7eaf0; --soft:#f7f9fc;        /* 경량 중립 */
--red:#c0392b; --amber:#c98a1a; --green:#1e8e5a;   /* 판정색: red▲/amber●/green✓ */
--blue:#3b86d1;                     /* 정보◆ = 스카이 통일 */
```

## 3. 헤더 규칙 (칙칙 해소 핵심)

- **본문 슬라이드 헤더 = 화이트.** `.shead{ background:#fff; border-bottom:1px solid var(--line); }`
  - 좌측 6px 골드 엣지(`.shead::before`)만 브랜드 유지
  - 아이브로우(`.skick`) = 스카이 `--sky-d` / 타이틀(`.stitle`) = 네이비 / 강조(`b`) = 골드 `--gold-d`
- **표지(cover)·마무리(closing) = 다크 유지.** 딥네이비/그래파이트 북엔드 (표준기준서 "표지=딥네이비 그라데이션").

## 4. 강조 규율 — "적을수록 전달된다"

- 한 슬라이드에 **강조색 1~2개**. 진한 색을 넓게 깔지 않는다(면=중립, 선·텍스트=액센트).
- 골드 = 브랜드 신호(엣지·룰·합계행), 스카이 = 데이터/정보, 골드·스카이 동시 남발 금지.
- 판정 pill·KPI 상단선에만 시맨틱색(red/amber/green). 배경 대면적 착색 지양.
- box-shadow:0 유지 — 깊이는 1px 헤어라인 + `--soft` 배경톤.

## 5. 적용 상태 / TODO

- [x] 양산 본문 9장 라이트 헤더 전환 + PDF 재렌더
- [ ] `시각카탈로그/` 두 파일 동기화(동일 토큰) — 필요 시
- [ ] 다른 현장(dangha-parking·ktower-geomdan) 리프레시 — 선택
- [ ] (보류) 결론부: "0원 검증"(자명) 대신 **분양가 범위·공사비 수준·초기 분양률 임계치**로 성패가 갈린다는 구체 결론 — 검증된 현금흐름표 확보 후
