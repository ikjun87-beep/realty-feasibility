# JN.md — 작업 진행노트

_최종 업데이트: 2026-07-13_

## 현재 상태 (어디까지 했나)
- **[2026-07-13] 화성 향남 장짐 — 검토본(deck 13장) 진행 중.** (`화성향남장짐/`, 상세는 **`화성향남장짐/JN.md`**)
  - deck 디자인 **Option 2 하이브리드(블루 라이트)** 전환 · 비교사례 **번호핀 지도**(`map_comps.png`) 신설 · 슬라이드 07(결론·산출논리) / 08(실거래 근거표) **역할 차별화**.
  - **이지그룹 4팀 검토 반영**: build-design·advisory-domain 완료 / advisory-cpa·qa-test는 **API 오류로 중단 → 재실행 필요**.
  - 핵심 확정: 엑셀 매출 역산으로 **분양가 1,400 = 공급기준** 확정(전용률 74.4%) → `[확인필요]`를 `[확인]`으로 격상, 판정 톤 완화(“확인/실현 가능” → “지표상 우호/여력”).
  - **다음 게이트는 법률(곽재원)** — 주택법 §15·16 사업계획승인 실효(’14 승인·12년 미착공)·미매입부지 매도청구. 법률 확인 전까지 리스크표 격상 임의 반영 안 함.
- **[2026-07-10] 강화 내리 3필지 — 지적검토 리포트 + 전원주택 가설계/3D.** (`ganghwa-naeri/`) 파이썬 빌드 스크립트(`build_report.py`·`fetch_landdeals.py`·`neighbors_map.py`) 기반. 커밋 `f09eda3`. 후속 요청 대기.
- **[2026-07-12] 양산 보고서 디자인 라이트 리프레시 — 완료.** (`cashflow-report/양산_사업수지_보고서.html/.pdf`, 9장)
  - 계기: 골드+다크네이비 헤더가 전 슬라이드에서 **칙칙·과강조 → 전달력 저하**(사용자 지적). 표준기준서 §9가 실은 **네이비 `#13243f` + 스카이 `#3b86d1`**를 추천함을 확인.
  - 조치: 본문 헤더 **다크 띠 → 화이트**(네이비 타이틀·스카이 아이브로우·골드 6px 엣지), 스카이블루를 데이터/정보 액센트로, 골드는 얇은 브랜드 액센트로 절제. `:root` 토큰 경량화(--sky·--sky-l 추가, --line·--mut·--soft 밝게).
  - **표지·마무리도 라이트로 통일**(기존 다크 그래파이트/웜 아크 폐기) → 표지→본문→마무리 전체가 화이트/네이비/스카이/골드 한 시스템. 다크 슬라이드 없음. 표지 배경 이미지(`cover_render_yangsan.png`) 미사용(오폰 잔존).
  - 신규 문서 `디자인_색감_가이드.md`(루트) — 3개 색 체계(표준§9·다방·구report) 충돌 정리 + 확정 토큰 + 헤더 규칙 + "강조 적을수록 전달" 규율.
  - **워터폴 슬라이드는 도입했다가 제거**(사용자: 이해도·일반성 낮음). 직전 세션 커밋 `b140762`(워터폴 추가)는 이번에 되돌림.
  - **[외부 카탈로그] `시각카탈로그/`** — @jay.pm.ai의 슬라이드골격+차트 사전(HTML 2종)을 하우스 톤으로 리스킨. **public 저장소 재배포 금지 라이선스**라 `.gitignore` **로컬 전용** 처리(커밋 안 함). Windows Downloads에 비교용 사본만 존재.
- **표준기준서 v2.2 — 사업수지(현금흐름) E형 모듈 신설.** (`분양성보고서-표준기준서.md`)
  - 양산에서 확립한 구조를 표준화: §2 유형E 추가 · **§5E 신설**(항등식 잔차0·멱등 / 8대 지출분류 A토지~H금융 / 하이브리드 신탁 판정 / F~K 수지목차 / 엔진출력→슬라이드 매핑 / 완판 가정 라벨 / 2인 리뷰 루브릭) · §12 품질게이트 항등식 체크 · §13 보강로그.
  - 항등식 검증: `이익 19,778,226 = 총수입 324,220,493 − 지출 304,442,267`(천원) 잔차0 ✓ (JN TODO 4번째 항목 완료).
- **PC 저장·자동 미러 체계 구축.** 원본은 WSL(`/home/jun/projects/realty-feasibility`, 탐색기 `\\wsl.localhost\Ubuntu\...`). Windows 정본 = `C:\Users\JUN\Claude\Projects\realty-feasibility` 한 곳으로 통일(중복 사본 삭제). `git push` 시 `.git/hooks/pre-push` 훅이 rsync로 자동 미러(비차단, node_modules 제외). → 메모리 `windows-mirror-on-push`.
- **양산 물금 사업수지 통합보고서 — 완료·확정.** (`cashflow-report/`)
  - PRD Phase P1~P6 전부 완료. 2인 심사역 리뷰 루프 최종 **A 55/55 + B 44/45 = 99/100**(목표 ≥98 달성).
  - 산출물: `양산_사업수지_보고서.html/.pdf`(11장 확정본) · `양산_사업수지_경영요약_1p.html/.pdf`(임원 1p) · `양산_사업수지_ppt_프롬프트.txt`(NotebookLM/슬라이드 도구용).
  - 항등식 잔차 0·멱등 검증 완료. 사업이익 197.8억(완판 100% 가정)·BEP 93.9%·순환급 38.6억 등 전 수치 엔진값과 정합.
  - 최신 커밋 `8820879`(PPT 프롬프트 추가). working tree clean.
  - 구글 드라이브 공유: `클로드 공유 폴더\양산_사업수지_ppt_프롬프트.txt`, `보고서 양식 만들기\양산_사업수지_보고서.pdf`.
- **당하동(`dangha-parking/`)·검단 K타워(`ktower-geomdan/`)** — 기존 완료 보고서(별도 현장). 이번 사이클에서 미변경.
- **작업노트 체계 도입** — 루트에 `CLAUDE.md`(고정정보)·`JN.md`(진행노트) 추가. 세션 시작 `/jngo`(JN 읽고 이어가기) · 마감 `/jnsave`(문서 갱신+커밋+푸시) 워크플로우 사용.

## 다음 할 일 (TODO)
- [ ] **(진행중·최우선) 화성 향남 장짐 확정본** — 법률 게이트(곽재원) → 리스크표 격상 → 회계(남주희)·QA(나정환) 재실행 → 분양사례 comp 보강 → PPT 프롬프트. **세부 체크리스트는 `화성향남장짐/JN.md`가 정본**(루트에 중복 기재하지 말 것).
- [ ] **(보류·우선) 양산 결론부 재작성** — "수지 0원 검증"(항등식상 자명)을 결론으로 강조하지 말 것. 대신 **최종 분양가 범위 · 공사비 수준 · 초기 분양률 임계치**로 사업 성패가 갈린다는 **구체 데이터 결론**으로. 전제: **검증된 현금흐름표**(다른 프로젝트에서 진행 중) 확보 후 그 데이터로 다시 작성.
- [ ] (선택) `시각카탈로그/` 두 파일을 라이트 토큰(`디자인_색감_가이드.md`)으로 동기화 — 로컬 전용이라 급하지 않음.
- [ ] (선택) 라이트 리프레시를 다른 현장(dangha-parking·ktower-geomdan)에도 적용.
- [ ] (요청 시) 새 현장 보고서 — 다른 지번을 동일 파이프라인(엔진 실행 → 11장 빌드 → 리뷰 루프 ≥98)으로 제작. **이제 표준기준서 §5E(E형 모듈) 기준으로 진행.**
- [ ] (요청 시) 양산 **분양률 시나리오 재산출** — 분양률·금리 가정 바꿔 엔진 재실행 후 민감도 보강(사용자가 가정값 지정 필요).
- [ ] (요청 시) 배포본 변환 — `pptx-designer`로 편집 가능한 .pptx, 또는 `hwpx-writer`로 한소 변환.
- [x] ~~표준기준서에 사업수지(현금흐름) 유형 모듈 반영~~ → **v2.2 §5E로 완료(2026-07-03).**
- [ ] 렌더 파이프라인 재현성 개선 검토(아래 이슈 참조).

## 주요 결정사항
- **HTML = SSOT, PDF = 파생.** 수정은 HTML에서만, PDF는 재렌더.
- 디자인 "다방" 규율: Pretendard 단일 · box-shadow:0 + 1px 헤어라인 유지. **[2026-07-12 변경] 색은 라이트 시스템으로 전환** — 네이비 `#13243f`(구조·타이틀) + 스카이 `#3b86d1`(데이터/정보 액센트) + 골드 `#b3873a`(절제된 브랜드 액센트), 화이트 배경. **표지·본문·마무리 전부 라이트**(기존 표지 쿨네이비↔마무리 웜 대비 폐기). 근거·토큰: `디자인_색감_가이드.md`. "강조 적을수록 전달된다"(한 슬라이드 강조색 1~2개).
- **엔진 산출값 = 권위값**, 항등식 잔차 0·멱등일 때만 채택. **가부 미판정·투자권유 아님**(지표만 제시, 판단은 심사자).
- 사업이익 197.8억에는 **"완판 100% 가정" 라벨** 명시(상한값 disclosure).
- 품질 게이트: 2인 심사역(A 수지 55 / B 디자인 45) 리뷰 루프를 **≥98 수렴까지** 반복(PRD §9).
- 렌더 스크립트(`render*.mjs`)·`package.json`은 로컬 전용으로 **gitignore**.
- 커밋은 **main 직접**, 메시지 끝 Co-Authored-By.

## 막힌 것 / 이슈
- ⚠ **렌더 스크립트 gitignore** → 새 클론에는 `render*.mjs`·`node_modules`가 없어 즉시 렌더 불가(로컬에서 재작성/재설치 필요). 재현성 필요하면 스크립트를 추적에 넣거나 별도 setup 문서화 검토.
- ⚠ **WSL scratchpad `chromelibs` 주기적 삭제** — 세션 중에도 정리되어 렌더가 `libnspr4.so` 오류를 냄. 발생 시 메모리 `render-chrome-no-sudo` 절차로 재구성(수 초).
- ⚠ **구글 드라이브(G:) WSL 미마운트** — `/mnt/g` 없음. 파일 입출력은 PowerShell 경유(한글 경로는 UTF-8 BOM .ps1 + `-LiteralPath` 사용, 그냥 `-Command`에 `$`쓰면 bash가 먼저 치환하니 주의).
- ℹ **NotebookLM 직접 연동 불가**(공개 API/커넥터 없음) — 소스 PDF 업로드 + 프롬프트 붙여넣기 수동 방식으로만 사용.
- ℹ **저장 3원(WSL 원본 · Windows 정본 · GitHub).** WSL이 SSOT, Windows 정본은 `pre-push` 훅 자동 미러(수정은 WSL에서만, Windows 사본 직접 수정 금지). 훅은 `.git/hooks/`라 gitignore·새 클론엔 없음 → 메모리 `windows-mirror-on-push`대로 재설치.
