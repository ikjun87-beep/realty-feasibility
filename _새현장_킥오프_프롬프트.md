# 신규 현장 검토 킥오프 프롬프트 (realty-feasibility)

> 새 부동산 검토 보고서를 시작할 때 이 파일 내용을 그대로 붙여넣거나 `@_새현장_킥오프_프롬프트.md` 로 불러오세요.
> 핵심 원칙: **realty 표준은 그대로 유지 · 대학로131의 새로운 부분만 얹기 · 충돌/중복은 임의 결정 말고 질문 · 자료는 업로드/경로 요청 후 진행.**

---

너는 지금 `realty-feasibility` 저장소에서 **새 현장 검토 보고서**를 시작한다. 아래 순서로 진행하되, 확정되지 않은 것은 절대 임의로 정하지 말고 나에게 물어라.

## 1. 기준 로드 — 이대로 유지
- 루트 `CLAUDE.md`, `분양성보고서-표준기준서.md`(목차·§4 필수체크리스트·§7 지도표준·§9 폼·§12 품질게이트), 디자인 규율 "다방"(Pretendard 단일·box-shadow:0·골드 `#b3873a`/그래파이트 네이비 `#1b2738`/그레이·16:9 297×167mm)을 **기본 표준으로 그대로 적용**한다.
- 이어서 `대학로131/JN.md`·`대학로131/CLAUDE.md`를 읽되, **디자인 골격은 이미 이 표준에서 파생된 것이므로 중복 도입 금지.** 아래 §2의 '신규 반영분'만 가져온다.

## 2. 대학로131에서 새로 얹을 것 (delta만)
- **톤(정보전달 목적)**: 이모지 금지(렌더 두부현상)·책임회피/면책 문구 금지·추정치는 라벨링(`[추정]`/`[확인필요]`).
- **레이아웃 QA**: 고정 높이(167mm) **오버플로·겹침 금지**, 좌우 컬럼 **상단 정렬**, 과밀 금지(슬라이드별 적정 하단 여백 허용).
- **공공데이터 차트**: CSS 막대(`.chart/.brow`)로 시각화하되 **숫자는 출처로 검증 가능한 값만**, 각 차트에 **출처·기간·단위** 명기. 검증 불가 수치는 넣지 말고 조회처만 안내.
- **페이지번호**: CSS 카운터 자동화(`counter(sp, decimal-leading-zero)`) — 페이지 추가/삭제에도 자동.
- **위치/지도**: 핀 좌표 추정 금지. 도로명 지오코딩은 **OSM/Nominatim 금지**(도로 중심점 반환) → VWorld/juso/Kakao + **실제 상호(POI) 교차검증**으로 확정.

## 3. 충돌·중복 지침은 질문으로 결정 (임의 결정 금지)
표준과 대학로131 관행이 갈리는 지점 — 작성 전 나에게 물어 확정하라:
1. **렌더 엔진**: 표준=Chrome(puppeteer, keep-all 네이티브; `chromelibs`/메모리 `render-chrome-no-sudo`) vs 대학로131=WeasyPrint(word-joiner 주입). → 어느 것?
2. **지도**: 표준=Leaflet→PNG 캡처(§7) vs 대학로131=실제 지도 스크린샷 크롭. → 어느 방식?
3. **보고서 유형/목차**: 분양성·사업성(표준 목차) vs 매입·리모델링·수익(대학로131 구성). → 어느 목차?
4. **산출물**: report(장문)·deck(16:9 슬라이드)·임원 1p·PPT 중 무엇을 만들까?

## 4. 자료·첨부 요청 — 여기서 멈추고 나에게 요청
새 보고서를 만들려면 아래를 **업로드**하거나, 이미 있으면 **파일 경로를 알려달라**고 나에게 요청하라. 받은 뒤에만 다음 단계로 진행:
- 현장 개요: 주소·지번·용도지역·대지/연면적·층수·준공연도
- 거래/사업 정보: 매입가(또는 분양가) 가정, 임대 현황, 재무 가정
- 첨부: 도면·감정평가서·등기부·건축물대장·상권/실거래 자료 등
- 없으면: 어떤 공개데이터로 채울지 합의(§5)

## 5. 도구 활용 (필요 시)
- **공공데이터 검증**: `moai-public-data`(real-estate-search 실거래가·court-auction-search·public-data), `moai-finance`, `WebSearch`/deep-research. 지오코딩은 VWorld/juso/Kakao.
- **자문 검토(이지그룹 에이전트)**: 재무·사업수지=`advisory-cpa`, 규제·계약·인허가=`advisory-lawyer`, 부동산 도메인(기본 신탁/부동산)=`advisory-domain`.
- **문서화**: office 스킬(`pdf-writer`/`pptx-designer`/`hwpx-writer`), 최종 텍스트는 `ai-slop-reviewer`로 마감.

## 6. 산출 구조
자료·결정이 모이면: `realty-feasibility/<현장명>/` 하위폴더 생성 →
- `JN.md`(대학로131 구성을 **템플릿으로 참고**: 현재상태/슬라이드구성/확정사실/다음할일/결정사항/이슈)
- `CLAUDE.md`(파일구조·렌더방법·디자인규격·관련메모리)
- `report.html` 또는 `deck.html`(**SSOT**) 스켈레톤 → 표준 목차·§12 품질게이트 따라 작성, PDF는 파생물.

---
**지금 할 일: (1) §1 기준을 읽고 → (2) §3 질문 4가지를 나에게 하고 → (3) §4 자료 업로드/경로를 요청하라. 내가 답·자료를 주기 전까지 보고서 작성을 시작하지 마라.**
