# CLAUDE.md — 화성 향남 장짐지구 공동주택 검토 (고정정보)

## 프로젝트
경기 화성특례시 향남읍 장짐리 228-1 일원, **공동주택 1,885세대 신축사업**의 분양성·사업성 검토.
루트 `../분양성보고서-표준기준서.md` 표준 준수. 최종 산출물 = **PPT 프롬프트**.

## 파일 구조
- `JN.md` — 진행상태·결정·다음할일 (**세션 시작 시 먼저 읽기**)
- `_원자료/` — 원본 4종(시장조사·현장보고서·PF IM·사업수지 xlsx)
- `_원자료/_txt/` — PDF 전체 텍스트 추출본 / `_원자료/_molit/` — 국토부 실거래 json(화성시 전체 12개월)
- `_원자료/추출_핵심팩트.md` — 팩트 정리 (⚠ **일부 stale** — "실거래 −26%"·"분양 comp" 오독 잔존, JN.md가 우선)
- `deck.html`(SSOT, 13장) → `향남장짐_분양성사업성_검토.pdf`
- `map_comps_kakao.html`·`map_locator_kakao.html` → `map_comps.png`·`map_locator.png`
- `fetch_infra.py` → `infra.json` (Kakao Local 반경 실측)

## 권위값 (사업수지 엑셀 VER13 · **천원**)
- 총수입 917,079,324 / 총지출 818,183,709 / **사업이익 98,895,615**
- 분양매출 868,033,339 · 분양가 **14,000천원/평 = 공급면적 기준 확정**
- **엑셀이 진실값.** 슬라이드 수치 하드코딩 금지, 엑셀→매핑.

## 단위·표기 규칙 (사용자 확정 · 필수)
- **금액·평단가 전부 천원.** 평단가는 `14,000천원/평` 형식, **공급면적 기준**. 만원·억원 혼용 금지.
- 아파트 평단가는 **공급면적**이 기본. 전용은 참고값으로만 병기.
- **비교사례는 최근 실거래(공급환산)** 사용 — 옛 분양가는 시점차 왜곡이라 금지.
- 어투 **개조식**(명사형 종결). "흡수" → **"분양물량 소화"**. "MOLIT 중위" → "국토교통부 실거래가 중간값".

## 검토 전제 (사용자 확정)
- **인허가 완료 · 토지 사용권원 확보를 전제.** 주택법 리스크(§16 승인취소·§21 매도청구)는 deck에 미표기.
- 제출용 문서 — 내부 주의사항·면책·"가부 미판정" 문구 금지.
- **결론은 사업성 있다/없다로 끝낼 것.** 분양가·사업이익을 크게 강조, 디테일은 하위.

## 렌더 / 도구
- 렌더: **Chrome/puppeteer**. chromelibs 없으면 메모리 `render-chrome-no-sudo` 절차 (**`~/chromelibs`에 재구성 — scratchpad는 주기적 삭제됨**).
- `render.mjs`(슬라이드 PNG) · `render_pdf.mjs`(PDF) · **`render_kakao.mjs`(카카오맵 전용)**
- **카카오맵**: JS 키 `.kakao_js_key`(gitignore) — REST 키(`.kakao_key`, 지오코딩용)와 **다른 키**.
  SDK가 `file://`을 거부하므로 **localhost:8766 서빙하며 캡처**해야 함.
  도메인 등록은 **앱 설정 → 플랫폼 키 → JavaScript 키 → JavaScript SDK 도메인** (제품 링크 관리 아님).
- 지오코딩: Kakao Local API. ⚠ **철도역은 카테고리 `SW8`에 일반철도가 안 잡힘** → 키워드 검색으로 확인.

## 규칙
- 미확보 수치 창작 금지. 추정치 `[추정]` 라벨.
- 커밋: main 직접, 메시지 끝 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
